apiVersion: apps/v1
kind: Deployment
metadata:
  name: notify-worker
  namespace: notify-hub
  labels:
    app: notify-worker
spec:
  replicas: 3
  selector:
    matchLabels:
      app: notify-worker
  template:
    metadata:
      labels:
        app: notify-worker
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/api/actuator/prometheus"
    spec:
      containers:
      - name: worker
        image: notify-hub/worker:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        env:
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: notify-db-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: notify-db-secret
              key: password
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: notify-redis-secret
              key: password
        envFrom:
        - configMapRef:
            name: notify-worker-config
        - name: JAVA_OPTS
          valueFrom:
            configMapKeyRef:
              name: notify-worker-config
              key: JAVA_OPTS
        command: ["java"]
        args:
        - $(JAVA_OPTS)
        - -jar
        - /app/notify-worker.jar
        volumeMounts:
        - name: logs
          mountPath: /logs
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /api/actuator/health
            port: 8081
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: logs
        emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - notify-worker
              topologyKey: kubernetes.io/hostname
