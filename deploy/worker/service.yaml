apiVersion: v1
kind: Service
metadata:
  name: notify-worker
  namespace: notify-hub
  labels:
    app: notify-worker
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: notify-worker
