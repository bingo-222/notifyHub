apiVersion: v1
kind: Service
metadata:
  name: notify-gateway
  namespace: notify-hub
  labels:
    app: notify-gateway
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: notify-gateway
